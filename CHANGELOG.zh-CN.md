# 更新日志

## 未发布

## 0.5.3 - 2026-07-11

- 修复 Tk 透明度生命周期在 v0.5.1 启动序列中清除覆盖层 Windows 工具窗口 Shell 身份的问题。
- 保持桌面覆盖层和通知区域伴随图标可见、可访问，同时从真实顶层 HWND 中移除普通应用窗口身份。
- 新增真实 HWND 回归覆盖：冷启动、设置、锁定、隐藏/显示、收缩/展开、透明度、缩放和置顶转换。

- 将文档重组为治理、架构、产品、质量、运维和归档层，并使用manifest驱动双语门禁。
- 新增仓库、配置、架构、测试、发布、安全、安装、故障排除和贡献指南。
- 新增manifest校验器并接入自动发布检查；当前测试套件通过88项测试并登记17组双语文档。
- 新增内部Markdown链接校验器并接入发布检查，防止文档迁移后留下失效导航。
- 新增经过校验的设置备份副本，并在右键菜单中提供恢复上一次设置的动作。
- 增强运行时窗口恢复：修正被任务栏部分遮挡的位置，并周期性检查显示器拓扑变化，同时在DPI舍入容差内保留合法副屏坐标。
- 新增带日期的实体测试记录目录，并在兼容性矩阵中链接当前 Windows 11 双屏拓扑证据。
- 将Tk主窗口拆分到 `scripts/ui/main_window.py`，同时保留 `scripts/codex_status_pet.py` 作为稳定启动器和导入外观层。
- 更新打包 smoke 和编译门禁，使其校验模块化主窗口的版本源和入口。
- 新增独立的版本来源一致性门禁，检查 manifest、主窗口、app-server 客户端和更新日志。
- 新增高置信度敏感文件和秘密材料扫描，并接入发布检查。
- 新增离线依赖门禁，在测试和打包前校验 requirements、最低版本和导入能力。
- 补充已测试Python/运行时基线、x64范围、待验证的Windows 10覆盖和未签名程序行为说明。
- 明确通知区域关闭操作为幂等，并新增重复stop回归测试。
- 扩展 Activity API 测试，覆盖多活动会话、最近完成、坏行和文件统计竞态。
- 新增配置schema v1迁移、旧文件规范化，以及未知未来版本的安全回退。
- 新增事务化设置会话：“应用”只预览运行时值，“保存”才持久化，“关闭”恢复打开设置时的快照。
- 新增统一整数输入校验，支持逐键输入负坐标并拒绝粘贴的非法格式。
- 新增可逆百分比缩放会话，加号和减号对宽高进行对称缩放。
- 将 Activity 与 Quota 刷新拆为独立 single-flight 通道，并加入过期 generation 和关闭保护。
- 新增严格额度解析、类型化额度领域模型以及最近成功/过期状态跟踪。
- 新增延迟收缩状态转换、悬停/活动展开、边缘锚定以及断开显示器后的最近工作区恢复。
- 删除经过测试的弹出菜单实现之后不可到达的旧原生菜单代码。
- 新增 Windows GitHub Actions 质量门禁，以及打包元数据和 smoke ZIP 检查。
- 将插件 manifest 的作者/开发者署名修正为 `Zixuan Tang`。
- 将右键菜单 Tk 适配层从主应用模块中拆出，同时保持原有行为契约不变。
- 将事务化设置窗口 Tk 适配层拆出，同时保持“应用/保存/关闭”语义不变。
- 将通知区域图标适配层拆出，同时保持所有动作通过 Tk 所有的队列处理。
- 将本地 Codex CLI 发现和 app-server stdio JSON-RPC 通信从 UI 组装模块中拆出。
- 新增可复制的诊断摘要菜单动作，并明确禁止令牌和内容数据进入摘要。
- 将状态文字/颜色格式化抽为不依赖 Tk 的展示 API。
- 在兼容性矩阵中记录最新 Windows 11 双屏启动器和单实例实体证据。
- 新增非阻塞发布就绪报告，列出仍阻止 v0.3.0 的实体门槛。
- 删除指向旧 `.agents` 副本的 Windows 启动快捷方式；当前仓库不会安装自动启动项。
- 记录临时隔离 venv 依赖安装、65 项测试和打包 smoke 成功；干净机器启动仍单独保留为门槛。
- 新增只读启动项审计，检测已知旧版 Codex 状态宠物项目，不修改无关启动项。
- 将启动项审计接入自动发布检查；当前主机报告没有旧版启动项。
- 将启动项审计扩展到 Run/RunOnce 注册表值中的旧名称和旧路径检测。
- 为 `probe_display.py` 增加任务栏边缘诊断；当前主机记录为底部，不推断其他边缘。
- 新增当前开发路线图及同步的中文翻译。
- 新增校验后的窗口尺寸、缩放模式和 1–10 秒刷新间隔设置。
- 新增弹出菜单工作区定位，以及纯额度日期/最近到期时间格式化 API。
- 新增独立的单实例刷新调度 API 及有限间隔测试。
- 新增六组英文/中文文档副本的结构一致性检查器。
- 新增纯额度健康状态 API，并加入健康/警告/危险悬浮窗颜色。
- 新增可选的空闲收缩和悬停展开，默认保持展开。
- 确保从收缩状态通过设置或托盘恢复时，主界面先恢复完整展开。
- 新增托盘生命周期策略 API，用于动作白名单和单次恢复调度。
- 将自由/等比例窗口缩放抽为独立并经过测试的 API。
- 新增中英文兼容性矩阵，并明确实体测试发布门槛。
- 新增仅限本地的额度数据源规范化 API，并明确禁止令牌边界。
- 新增可重复的自动化发布门禁运行器，并将实体测试保持为独立门槛。
- 在中英文开发计划和 API 规范中加入 Git/GitHub 变更纪律。
- 修复 Windows 编辑器和 PowerShell 常见 UTF-8 BOM 设置文件的读取问题。
- 新增右下角菜单定位、副屏幕和额度格式化回归测试。

## 0.5.1 - 2026-07-11

- 通过统一使用已定位 HWND 的 DPI 计算几何与像素字体，稳定同一个长生命周期悬浮窗在设置、锁定、显示、紧凑、恢复默认值和混合 DPI 显示器切换后的布局。
- 新增生产等价自动回归，覆盖 DPI 96/120 下全部 25 个缩放档位，以及暴露 v0.5.0 裁切假阳性的完整运行时转换序列。

## 0.5.0 - 2026-07-11

- 删除未使用的额度领域模型、历史自由缩放和历史缩放会话模块；这些模块此前只被实现细节测试引用。
- 删除单函数额度数据源透传，并将现有主窗口工作线程直接连接到严格批准字段额度解析器。
- 保留令牌/未知字段排除、损坏输入行为、schema-1 降级几何、规范窗口缩放及全部受保护 UI/生命周期契约。
- 将运行时生产 Python 从 39 文件/2,742 行减少到 35 文件/2,650 行，API 模块从 32 减少到 28；运行时依赖仍为 2。

## 0.4.2 - 2026-07-11

- 新增机器校验的发布事实清单，将事实分类为 automated、automatable、physical-only、obsolete 或 duplicate，并为每项事实指定唯一权威检查。
- 将 Windows CI 收敛到唯一正式 Release Candidate runner，不再把 Quality、打包 smoke 和空白检查作为独立工作流步骤重复执行。
- 将发布输出拆分为通过项、阻塞项和非阻塞限制，并使 Windows 子进程诊断稳定使用 UTF-8。
- 用确定性 Tk/Win32/进程证据或一条明确实体限制替代泛化人工 UI 确认要求。
- 未新增产品运行时功能、依赖、数据源、网络路径、轮询、遥测、配置字段或凭据访问。

## 0.4.1 - 2026-07-11

- 按窗口有效 DPI 缩放运行时像素几何、内边距、间隔和换行长度，在保持 96-DPI 逻辑配置值与 Tk 点制字体大小不变的前提下，修复全部 25 个“窗口大小”档位中的重置额度行裁剪。
- 用生产初始化顺序的 DPI-aware 子进程回归测试替换仅覆盖 96 DPI 的假阳性映射 Tk 检查；该测试在 120 DPI 下检查窗口、状态容器和五个状态行的真实/请求几何，包括完整的 Reset Credit 日期和时间行。
- 将托盘和额度传输失败统一路由到权威五行展示边界，不再对 `StatusRows` 使用无效的旧式 Label 配置。
- 保留真实的最近成功/过期额度行为，不再显示原始传输异常文本，并恢复托盘失败后的单次调度恢复。
- 未新增功能、依赖、数据源、网络路径、工作线程、轮询、遥测、配置字段或 schema 变更。

## 0.4.0 - 2026-07-11

- 用一个 80–200%“窗口大小”滑块替换独立字体大小、自由宽高、加减按钮和等比例模式控件；透明度仍是唯一另一个滑块。
- 新增一个纯 Window Scale API，从规范百分比推导固定 330:138 比例几何、自适应文字/爪印字体、换行和间距。
- 保持配置 schema 1，通过几何平均面积确定性迁移旧几何，并持久化派生旧字段，使 v0.3.2 降级后仍能读取可用尺寸。
- 在 Windows 主机 80/100/150/200 验证中保持“应用/保存/关闭/默认值”事务语义、五个稳定行、位置恢复、隐藏/恢复、收缩/展开、拖动/锁定、菜单定位和重启持久化。
- 缩放功能没有新增网络、IPC、工作线程、子进程、轮询、遥测、依赖、凭据路径或 Codex 额度消耗。

## 0.3.2 - 2026-07-10

- 使用带日期的单/双显示器、启动器、单实例、状态行、重置额度日期、菜单、隐藏/恢复和 Compact 交互证据，关闭已声明 Windows 11 x64 范围的实体发布门。
- 在 127 项自动化测试、文档和依赖门、打包 smoke 及可执行兼容性就绪检查全部成功后，批准严格 Release Candidate。
- 对不声明支持的 Windows 10、其他任务栏边缘、混合 DPI 实体环境和独立干净机器场景，继续保留明确的非阻塞限制。

## 0.3.1 - 2026-07-10

- 从 Tk 主窗口提取纯应用、状态展示、设置持久化和窗口生命周期控制器。
- 保持 Activity/Quota 时序与 generation 行为、收缩决定、受保护配置/重置语义、五行渲染和幂等关闭不变。
- 在将所有权移出 `Pet` 的同时，为现有刷新、调度、收缩状态和设置路径集成保留兼容视图。

## 0.3.0 - 2026-07-10

- 用五个持久、独立更新的状态行替换单一多行 Label：活动状态、活动对话进度、5h 额度、周额度和重置额度。
- 新增纯稳定行快照契约，同时为现有调用者保留逐字节兼容的拼接状态文字。
- 在所有状态行上保留收缩隐藏/恢复、拖动、悬停、右键、样式、换行、刷新和控制器行为。

## 0.2.6 - 2026-07-10

- 新增一个聚焦的可执行文档治理检查：`Goal/ACTIVE_GOAL.md` 是唯一规范 Goal，Goal 目录使用严格白名单。
- 要求归档计划声明已归档/非规范 front matter 和有效替代目标；归档 manifest 条目不能阻塞发布。
- 明确不增加归档双语一致性、时效性、文风、审查周期及其他低价值文档门禁。

## 0.2.5 - 2026-07-10

- 用不做发布就绪决定的日常 Quality runner 替换含义模糊的自动发布 runner。
- 新增严格 Release Candidate runner，要求 Quality、打包 smoke、空白检查和具有阻塞性的实体兼容性检查全部通过。
- 将 GitHub Actions 分为 push/PR Quality 和手动 Release Candidate 工作流；只有严格批准后才上传候选制品。

## 0.2.4 - 2026-07-10

- 在 README、安装、发布、产品、路线图和兼容性文档中统一 Windows 11 x64 受支持平台声明。
- 将 Windows 10 归类为延后、不声明支持、非阻塞，不再作为可执行发布阻塞项；ARM64 和 32 位 Windows 仍不声明支持。
- 更新发布就绪评估，单独报告明确的非阻塞项目，同时继续将每个待验证或部分完成的 Windows 11 x64 证据缺口作为阻塞项。

## 0.2.3 - 2026-07-10

- 防止所有常规保存路径覆盖未来 schema、损坏、非对象或字段无效的配置，包括拖动、隐藏、切换、窗口恢复和退出。
- 新增配置源兼容性元数据，同时保留原有的设置/警告解包契约。
- 仅允许通过明确的“恢复默认值”后“保存”流程替换受保护配置；受保护写入失败不会创建备份或部分写入。

## 0.2.2 - 2026-07-10

- 修复已批准嵌套数据形态的重置额度到期时间规范化，包括 `credits[].expiresAt` 和 snake_case 别名。
- 重置额度行现在按 `重置 N 次 / HH:MM M/D` 显示最近未来到期时间；缺少有效到期数据时不显示虚构后缀，5h 行仍然只显示时间。
- 新增有界白名单解析和端到端回归测试，不暴露未知字段或类似凭据的字段。

## 0.2.1 - 2026-07-10

- 将悬浮窗右键菜单精简为严格五项：显示设置、置顶、锁定位置、隐藏窗口和退出。
- 从右键菜单移除立即刷新、复制诊断摘要和恢复上次设置，同时保留自动刷新及内部诊断/配置机制。
- 新增菜单精确契约、首次点击单次分发、立即关闭和 Escape 关闭的回归测试。

## 0.2.0 - 2026-07-10

- 增加配置、活动状态、运行时、诊断和测试边界 API，并记录重大行为及性能变更规范。
- 将单实例启动改为命名互斥体，并增加无控制台模式下的诊断日志。
- 加强配置校验、活动状态超时和 PowerShell 启动器检查。
- 用可测试的 Tk 弹出适配层替代原先首次点击不稳定的原生菜单路径。
- 移除主界面的计划步骤显示，第二行现在始终只显示活动对话数量。
- 增加旧进程清理和命名互斥体，重复启动后最多保留一个悬浮窗和托盘图标。
- 重做主界面右键菜单，第一次点击即可执行并立即关闭菜单。
- 增加 Windows 任务栏通知区域集成，支持显示、隐藏、设置和退出。
- 启动器和登录启动快捷方式改用 `pythonw.exe`，不再保留控制台窗口。
- 增加透明度、颜色、字体大小、置顶、锁定位置和多显示器 X/Y 坐标持久化。
- 将工具调用计数改为活动对话数量显示。
- 修复隐藏窗口恢复问题，并保留虚拟桌面坐标。
- 修复右键菜单命令分发和 Tk 主线程调度问题。
- 增加中英文 README、文件规范和更新日志。

## 0.1.0 - 2026-07-09

- 初始 Windows 外部伴侣实现。
- 增加本机 Codex app-server 额度轮询和会话活动判断。
- 增加可拖动桌面悬浮窗和基础设置菜单。
