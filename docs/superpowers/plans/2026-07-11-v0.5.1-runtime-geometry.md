# v0.5.1 Runtime Geometry Reapplication Stabilization Implementation Plan

> **For agentic workers:** Execute this plan task-by-task in the current session using only Tom's allowed Superpowers workflows: systematic-debugging, test-driven-development, and verification-before-completion. Do not invoke unapproved execution or subagent skills.

**Goal:** Keep all five expanded status rows fully visible for one long-lived `Pet` across cold start and every supported settings, lock, visibility, and compact-mode transition.

**Architecture:** Keep `Pet._sync_compatibility_metrics()` as the sole target-window-DPI geometry authority. Position the withdrawn HWND before final widget construction, then express fonts as explicit target-DPI pixel sizes so geometry and typography consume one DPI value; later reapplications repeat the same authority without adding a subsystem.

**Tech Stack:** Python 3.12, tkinter/Tk, Win32 per-monitor DPI APIs, `unittest`, existing Windows probe and release scripts.

## Global Constraints

- Patch version is exactly `0.5.1`; v0.6.0 productization remains paused until release reconciliation completes.
- Preserve one long-lived production-equivalent `Pet`; a fresh instance per scale is not authoritative for transition stability.
- Cover cold start, lock toggle, settings open/close, Apply, Save, draft rollback, Restore Defaults, repeated settings cycles, Hide/Show, Compact/Expand, and the combined lifecycle.
- Cover every supported 80–200 scale step at effective DPI 96 and 120.
- Do not add arbitrary pixels, padding, persisted physical dimensions, a framework, a new controller, or an unrelated refactor.
- Logical compatibility width/height remain 96-DPI values; runtime physical geometry and fonts use the current target-window DPI.
- Machine-observable facts are verified automatically; Human Interaction Admission Gate is `NONE` for this patch.
- Run complete diff review, unrelated-change check, and secret scan before remote operations.
- Routine GitHub push/PR/CI/squash merge/tag/Release/merged-branch cleanup is pre-authorized only for `TomTang701/codex-windows-status-pet` and this ACTIVE_GOAL.

---

## File Map

- `tests/runtime_geometry_transition_probe.py`: production-equivalent diagnostic harness and machine-readable transition snapshots.
- `tests/test_ui_runtime_geometry_transitions.py`: authoritative automated RED/GREEN contract for one long-lived `Pet` and the DPI/scale matrix.
- `scripts/ui/main_window.py`: only production implementation file; owns withdrawn preposition, target DPI, pixel-font conversion, and reapplication.
- `Goal/EXECUTION_STATE.md`: current phase and exact next action.
- `Goal/ACTIVE_VERSION_BRIEF.md`: final v0.5.1 acceptance evidence and release state.
- `docs/quality/test-records/2026-07-11-v0.5.1-runtime-geometry-investigation.md`: root-cause evidence, RED/GREEN commands, RC, and remote reconciliation.
- `docs/quality/COMPATIBILITY_MATRIX.md` and `.zh-CN.md`: bilingual v0.5.1 compatibility row.
- `CHANGELOG.md` and `CHANGELOG.zh-CN.md`: bilingual patch release entry.
- Version-bearing files discovered by `scripts/check_version_sources.py`: set every checked source to `0.5.1` only after runtime GREEN.

### Task 1: Convert the Runtime Reproduction into an Authoritative RED

**Files:**
- Modify: `tests/runtime_geometry_transition_probe.py`
- Create: `tests/test_ui_runtime_geometry_transitions.py`
- Modify: `Goal/EXECUTION_STATE.md`

**Interfaces:**
- Consumes: `Pet`, the probe snapshot schema, and the already observed cold-start/toggle sequence.
- Produces: `run_transition_sequence(scales, dpi_values) -> dict` or an equivalent import-safe probe entry point whose records expose `transition`, `scale_percent`, `dpi`, `geometry`, `row_count`, `final_row_bottom`, `status_height`, and `fit`.

- [ ] **Step 1: Refactor the probe without changing behavior**

  Move script-only setup behind callable functions and retain the CLI JSON artifact. Ensure importing the module does not launch Tk or mutate the user's real settings.

- [ ] **Step 2: Write the focused long-lived regression**

  Add a `unittest` that creates one `Pet`, verifies cold fit, performs exactly `toggle_locked`, and then requires unchanged scale/DPI to preserve expanded geometry and five-row fit:

  ```python
  self.assertTrue(cold["fit"])
  self.assertEqual(toggle["scale_percent"], cold["scale_percent"])
  self.assertEqual(toggle["dpi"], cold["dpi"])
  self.assertEqual(toggle["geometry"], cold["geometry"])
  self.assertEqual(toggle["row_count"], 5)
  self.assertTrue(toggle["fit"])
  self.assertLessEqual(toggle["final_row_bottom"], toggle["status_height"])
  ```

- [ ] **Step 3: Run RED against released behavior**

  Run:

  ```powershell
  python -m unittest tests.test_ui_runtime_geometry_transitions.RuntimeGeometryTransitionTests.test_toggle_preserves_cold_start_fit -v
  ```

  Expected: FAIL because cold geometry is `330x138` and the same instance becomes `264x110`, with the final row clipped after `toggle_locked`.

- [ ] **Step 4: Record the RED and checkpoint**

  Update `Goal/EXECUTION_STATE.md` with the exact failing assertion and command. Review `git diff --check`, then commit only the test/harness/state files as `test: lock v0.5.1 runtime geometry regression`.

### Task 2: Establish One Target-Window-DPI Geometry Authority

**Files:**
- Modify: `scripts/ui/main_window.py:108-166,226-260`
- Test: `tests/test_ui_runtime_geometry_transitions.py`
- Test: `tests/test_ui_menu.py`

**Interfaces:**
- Consumes: `dpi_for_window(hwnd) -> int`, `derive_window_metrics(scale_percent, dpi) -> WindowMetrics`.
- Produces: `Pet.window_dpi: int`; `Pet._font_spec(family: str, logical_point_size: int) -> tuple[str, int]`, where the size is negative target-DPI pixels.

- [ ] **Step 1: Add focused font-authority tests while production remains RED**

  Assert DPI 96 maps logical 10 pt to `("Segoe UI", -13)`, DPI 120 maps it to `("Segoe UI", -17)`, and non-positive DPI falls back through the existing DPI API contract. Assert compatibility settings still store logical 96-DPI width/height.

- [ ] **Step 2: Run the focused tests and confirm expected failure**

  Run:

  ```powershell
  python -m unittest tests.test_ui_runtime_geometry_transitions tests.test_ui_menu -v
  ```

  Expected: the new `_font_spec`/transition assertions fail; unrelated existing UI tests pass.

- [ ] **Step 3: Implement withdrawn target positioning**

  In `Pet.__init__`, immediately withdraw the root, load settings, derive provisional metrics only for recovery bounds, recover the saved coordinate, issue position-only `self.geometry(f"+{x}+{y}")`, and call `update_idletasks()`. Then call `_sync_compatibility_metrics` again so final geometry is derived from the target monitor before labels are created. Deiconify only after widgets, settings, tray scheduling, and runtime state are initialized.

- [ ] **Step 4: Implement explicit pixel-font conversion**

  Make `_sync_compatibility_metrics` store the positive `dpi_for_window` result as `self.window_dpi`. Add:

  ```python
  def _font_spec(self, family, logical_point_size):
      pixels = max(1, round(logical_point_size * self.window_dpi / 72.0))
      return family, -pixels
  ```

  Use this helper for both label construction and every `apply_settings` reconfiguration. Do not modify global `tk scaling`.

- [ ] **Step 5: Run GREEN and existing UI regressions**

  Run:

  ```powershell
  python -m unittest tests.test_ui_runtime_geometry_transitions.RuntimeGeometryTransitionTests.test_toggle_preserves_cold_start_fit -v
  python -m unittest tests.test_ui_menu tests.test_ui_status_rows -q
  ```

  Expected: all commands exit 0; the same-Pet cold/toggle geometry and five-row fit remain identical.

- [ ] **Step 6: Review and checkpoint**

  Inspect the complete production diff for initialization-order regressions, hidden startup flashes, and accidental ownership changes. Run `git diff --check`, then commit as `fix: stabilize runtime geometry reapplication`.

### Task 3: Expand the Long-Lived Transition and DPI/Scale Matrix

**Files:**
- Modify: `tests/test_ui_runtime_geometry_transitions.py`
- Modify: `tests/runtime_geometry_transition_probe.py`
- Modify: `docs/quality/test-records/2026-07-11-v0.5.1-runtime-geometry-investigation.md`

**Interfaces:**
- Consumes: the fixed `Pet` and probe snapshot contract from Task 1.
- Produces: parameterized assertions for 25 scales at DPI 96/120 and every required transition.

- [ ] **Step 1: Add the transition matrix tests**

  For one instance per effective DPI, iterate scales `range(80, 201, 5)` and run every required lifecycle transition. After each transition assert five rows, fit, no unexpected wrapping for approved stable rows, exact `derive_window_metrics(scale, dpi)` expanded dimensions, and logical compatibility dimensions from `derive_window_metrics(scale, 96)`.

- [ ] **Step 2: Add position and lifecycle invariants**

  Assert legal secondary coordinate `(4151,1248)` is preserved, Hide/Show retains authoritative dimensions, Compact/Expand returns to them, settings rollback restores prior values, and repeated settings cycles do not drift geometry or create another root.

- [ ] **Step 3: Run the complete focused matrix**

  Run:

  ```powershell
  python -m unittest tests.test_ui_runtime_geometry_transitions -v
  python tests/runtime_geometry_transition_probe.py
  ```

  Expected: all tests pass and every generated transition record has `fit: true`; the artifact identifies one process and one long-lived `Pet` per DPI case.

- [ ] **Step 4: Record evidence and checkpoint**

  Append exact counts, DPIs, scale count, transition count, geometry invariants, and any approved physical-topology limitation to the investigation record. Run `git diff --check` and commit as `test: cover runtime geometry transition matrix`.

### Task 4: Patch Metadata, Bilingual Documentation, and Release Evidence

**Files:**
- Modify: version-bearing files reported by `python scripts/check_version_sources.py`
- Modify: `CHANGELOG.md`
- Modify: `CHANGELOG.zh-CN.md`
- Modify: `docs/quality/COMPATIBILITY_MATRIX.md`
- Modify: `docs/quality/COMPATIBILITY_MATRIX.zh-CN.md`
- Modify: `Goal/ACTIVE_VERSION_BRIEF.md`
- Modify: `Goal/EXECUTION_STATE.md`
- Modify: `docs/quality/test-records/2026-07-11-v0.5.1-runtime-geometry-investigation.md`

**Interfaces:**
- Consumes: verified Task 3 results.
- Produces: exact version `0.5.1`, synchronized English-primary/Chinese-copy documentation, and release-candidate evidence without premature completion claims.

- [ ] **Step 1: Update version sources and release notes**

  Set every checker-governed version source to `0.5.1`. Add a concise English changelog entry describing the runtime geometry/DPI root-cause fix and automated long-lived regression; translate it faithfully into the Chinese copy.

- [ ] **Step 2: Update compatibility and Goal state**

  Add matching bilingual matrix rows. Mark implementation complete but RC/remote reconciliation pending; do not mark the ACTIVE_GOAL complete.

- [ ] **Step 3: Verify document and version contracts**

  Run:

  ```powershell
  python scripts/check_version_sources.py
  python scripts/check_doc_parity.py
  python scripts/check_doc_manifest.py
  python scripts/check_doc_links.py
  ```

  Expected: every command exits 0.

- [ ] **Step 4: Review and checkpoint**

  Compare each Chinese document against its English authority, run `git diff --check`, and commit as `docs: prepare v0.5.1 release evidence`.

### Task 5: Full Verification, Package, and Formal Release Candidate

**Files:**
- Modify only if evidence reveals a requirement gap; any failure returns to systematic debugging and focused TDD.
- Modify: `docs/quality/test-records/2026-07-11-v0.5.1-runtime-geometry-investigation.md`

**Interfaces:**
- Consumes: complete v0.5.1 branch.
- Produces: fresh Quality/package/RC evidence and reviewed release diff.

- [ ] **Step 1: Run focused host verification**

  Launch the current source through the root launcher, verify exactly one process/tray instance and source provenance, then run the long-lived transition probe at the real saved secondary-monitor coordinate. Terminate only the test-owned process.

- [ ] **Step 2: Run the formal gates**

  Run:

  ```powershell
  python scripts/run_quality_checks.py
  python scripts/package_smoke_test.py
  python scripts/run_release_candidate_checks.py
  git diff --check
  ```

  Expected: Quality approved; package smoke exits 0; RC reports `release_candidate_approved: true`, zero blockers, and only already-classified limitations; whitespace clean.

- [ ] **Step 3: Perform completion review**

  Review `git diff main...HEAD`, `git status --short`, changed-file scope, secrets/credentials, generated artifacts, version consistency, and all requirements in the v0.5.1 brief. Record exact commands, counts, hashes, passes, blockers, and limitations.

- [ ] **Step 4: Commit verified evidence**

  Commit the final evidence-only update as `docs: record v0.5.1 release candidate` after rerunning document parity and `git diff --check`.

### Task 6: Standing-Authorized GitHub Release and State Reconciliation

**Files:**
- Modify: `Goal/ACTIVE_GOAL.md`
- Modify: `Goal/ACTIVE_VERSION_BRIEF.md`
- Modify: `Goal/EXECUTION_STATE.md`
- Modify: `docs/product/ROADMAP.md`
- Modify: `docs/product/ROADMAP.zh-CN.md`
- Modify: `docs/quality/test-records/2026-07-11-v0.5.1-runtime-geometry-investigation.md`

**Interfaces:**
- Consumes: exact locally verified branch HEAD and standing authorization.
- Produces: merged `main`, tag/Release `v0.5.1`, cleaned release branch, reconciled Goal state, and resumed v0.6.0 design phase.

- [ ] **Step 1: Verify remote identities and push exact branch head**

  Run `git remote -v`, `gh auth status`, `git config user.name`, and `git config user.email`; require target owner `TomTang701` and valid non-`tangz` author identity. Push `fix/v0.5.1-runtime-geometry` without force.

- [ ] **Step 2: Create PR and monitor exact-head CI**

  Create a PR containing Summary, Main changes, Verification, and Limitations. Verify PR head SHA equals the pushed local SHA; monitor `Windows Quality / quality`. On failure, read logs and return to systematic debugging/TDD before pushing a focused repair.

- [ ] **Step 3: Squash merge and verify main**

  After all required checks pass, squash merge, fetch/switch/pull `main`, verify merged contents and exact merge SHA, then rerun `python scripts/run_release_candidate_checks.py` on merged `main`.

- [ ] **Step 4: Tag, Release, and clean branch**

  Create and push annotated tag `v0.5.1` at the verified merged-main commit, create the matching GitHub Release from the English changelog, verify tag/Release target, and delete the completed local/remote patch branch.

- [ ] **Step 5: Reconcile the Goal and resume v0.6.0 design**

  Record PR, CI run, merge SHA, merged-main RC, tag, Release URL, and branch cleanup. Mark the v0.5.1 patch complete, correct both roadmaps, and set the next active phase to v0.6.0 productization design only after all remote facts are verified. Commit/push this reconciliation through the same authorized PR workflow if it is not already part of the release commit.

## Self-Review

- Spec coverage: every requested transition, 25 scales, DPI 96/120, false-positive probe gap, target-DPI authority, package/RC, and full remote reconciliation has an owning task.
- Placeholder scan: no deferred implementation placeholders; conditional failure handling explicitly routes back to systematic debugging/TDD.
- Interface consistency: `window_dpi`, `_font_spec`, probe snapshot keys, logical/physical metric distinction, version `0.5.1`, and release commands are consistent throughout.
- Scope check: one bounded Tk geometry subsystem and one patch release; v0.6.0 work remains outside implementation scope until reconciliation.
