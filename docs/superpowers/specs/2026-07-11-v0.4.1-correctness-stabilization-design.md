# v0.4.1 Correctness Stabilization Design

> **Current status:** Revised after Tom's 2026-07-11 production screenshot invalidated the original 96-DPI clipping verification. The DPI-aware revision at the end of this document is authoritative.

## Outcome

v0.4.1 closes two released-product correctness gaps without adding a feature:

1. every approved status row, including the full reset-credit date/time row, fits at 80%, 100%, 150%, and 200%;
2. tray and quota transport failures render through the existing presentation boundary instead of calling Label-only options on the `StatusRows` frame.

## Problem evidence

### Status-row clipping

A real Tk probe instantiated the production `Pet`, applied the approved text `重置 5 次 / 18:40 7/12`, mapped the window, and measured actual/requested geometry:

| Scale | Window | Window requested | Status allocated/requested | Reset row allocated/requested | Final row bottom |
|---:|---:|---:|---:|---:|---:|
| 80% | 264×110 | 173×111 | 204×94 / 113×95 | 204×18 / 113×19 | 102 |
| 100% | 330×138 | 208×135 | 256×118 / 134×115 | 256×23 / 134×23 | 125 |
| 150% | 495×207 | 308×200 | 386×177 / 199×170 | 386×34 / 199×34 | 185 |
| 200% | 660×276 | 413×255 | 517×236 / 270×215 | 517×43 / 270×43 | 235 |

At 80%, scaled height is `round(138 × 0.8) = 110`, while Tk requests 111. The status container loses one pixel and the final row receives 18 of its requested 19 pixels. A subsequent all-25-step probe found the same class of clipping at 95%: the 131 px window receives a 135 px request because text font and vertical padding both round up. Every other supported step fits with the original geometry. The current scale tests check geometry, font, wraplength, padding, identity, and lifecycle, but not actual/requested content fit.

### Legacy error rendering

Two production paths execute Label-style calls against a `StatusRows(tk.Frame)`:

```python
self.text.config(text="Codex\n托盘图标异常", fg="#fca5a5")
self.text.config(text="Codex\n" + payload["error"][:30], fg="#fca5a5")
```

Integration probes produced the following results:

- `tray_error`: one logged Tk exception, unchanged five rows, and `tray_restart_scheduled == False` because the exception aborts before restart scheduling;
- quota transport error: one logged Tk exception, unchanged five rows, quota domain state changed to `unavailable`, and `last_error == "transport_error"`.

## Root-cause hypotheses

### Layout

Independent integer rounding is asymmetric with Tk font metrics at 80% and 95%. At those two steps, the normal vertical padding leaves less height than five mapped single-line labels request. Reducing only those measured padding values to 7 px and 8 px respectively preserves canonical geometry, aspect ratio, fonts, configuration compatibility, and complete content. This is not a missing row, long-text wrapping problem, or monitor geometry problem.

### Error presentation

The v0.3.0 migration replaced one Label with a five-row Frame but left two emergency direct-string calls unchanged. `tk.Frame.config` does not accept Label options `text` or `fg`, so Tcl raises before the intended visible update and, for tray failure, before restart scheduling.

## Considered approaches

### A. Content-safe measured padding plus existing presentation path — superseded

- Preserve existing expanded geometry and use measured vertical padding overrides only at 80% (`7`) and 95% (`8`).
- Add a mapped-Tk content-fit contract at all 25 supported scale steps.
- Teach the existing status snapshot/controller boundary to produce approved failure rows.
- Apply every visible result through `StatusRows.configure_rows`.

This fixes both measured deficits at the responsible layout metric, preserves deterministic geometry and compatibility, adds no module/dependency, and consolidates rather than duplicates presentation.

### B. Increase expanded height — rejected

Increasing height fixes 80% but either changes many compatible derived heights through global ceiling or distorts the protected aspect ratio at 95%. Full-range evidence shows the existing geometry is sufficient when the two independently rounded padding values are corrected.

### C. Dynamically resize from Tk requested geometry — rejected

Querying mapped widget dimensions and mutating geometry at runtime would create a second size source, add geometry-loop risk, and weaken the pure canonical scale contract. The measured deficit is deterministic and does not justify runtime adaptation.

### D. Full runtime-state redesign — rejected

A new notice/state framework could model every failure, but v0.4.1 needs only two known paths. Phase 4 is explicitly conditional; introducing a broad state abstraction now violates scope and the new-abstraction budget.

## Observable contract

### Content fit

For each scale in `(80, 100, 150, 200)`, after mapping the production window with approved five-row text:

- root actual width/height equal `derive_window_metrics(scale)`;
- `root.winfo_reqheight() <= root.winfo_height()`;
- `StatusRows.winfo_reqheight() <= StatusRows.winfo_height()`;
- every label has positive actual height and `winfo_reqheight() <= winfo_height()`;
- every label's bottom is within the `StatusRows` actual height;
- the `reset_credit` label's right edge and bottom are within the status container;
- its requested width is no greater than actual width and its requested height equals one line's requested height;
- Compact/Expand and Hide/Show restore the same expanded metrics and retain all five row identities.

Expanded geometry remains 264×110, 330×138, 495×207, and 660×276 at 80/100/150/200%. The complete test iterates every 5% step and verifies content fit.

### Error presentation

- A tray error produces stable rows with activity `Codex`, progress `托盘图标异常`, approved error color `#fca5a5`, and schedules exactly one tray restart.
- A quota transport error with no last-good snapshot produces stable rows with activity determined by current activity, progress `额度暂不可用`, unavailable quota rows, and approved error color; raw exception text is not displayed.
- A quota transport error with last-good data retains the existing `QuotaState` truthfulness rule (`ok` inside freshness threshold, `stale` after it) and does not replace quota data with invented values.
- Normal, stale, unavailable, tray-error, and transport-error results all cross `StatusPresentationController` and reach Tk through `StatusRows.configure_rows`.
- No `self.text.config(text=..., fg=...)` path remains.

## Failure-path map

```text
window_scale_percent
→ derive_window_metrics
→ Pet.geometry and pack metrics
→ mapped StatusRows allocation
→ five Label allocations
→ complete single-line reset-credit row

tray_error queue action
→ Pet.process_tray_actions on Tk main thread
→ StatusPresentationController tray-error result
→ StatusRows.configure_rows
→ approved visible rows
→ should_schedule_restart

quota worker exception
→ production result queue
→ Pet.poll on Tk main thread
→ QuotaState.fail
→ StatusPresentationController normal/error-state render
→ StatusRows.configure_rows
→ truthful unavailable/stale result
```

Workers continue to enqueue data only and never call Tk.

## Interfaces and ownership

### `scripts/api/window_scale_api.py`

`derive_window_metrics(value) -> WindowMetrics` remains the only expanded metric source. Geometry rounding is unchanged. `CONTENT_SAFE_VERTICAL_PADDING = {80: 7, 95: 8}` replaces only the two measured unsafe padding results. No new configuration field or schema change is introduced.

### `scripts/api/status_snapshot_api.py`

`build_status_snapshot(...)` remains the normal quota/activity mapper and adds explicit approved progress/color mapping for unavailable transport state without exposing exception text.

Add a narrowly bounded pure function:

```python
build_tray_error_snapshot(font_color="#fca5a5") -> dict
```

It returns the same presentation shape as `build_status_snapshot`: `text`, `rows`, `color`, `active_count`, `quota_tier`, and `quota_state`. Rows remain exactly the five stable identities.

### `scripts/api/status_presentation_controller_api.py`

Add:

```python
render_tray_error() -> dict
```

It delegates to the pure snapshot helper. The controller remains the single presentation boundary; no new manager/controller/module is introduced.

### `scripts/ui/main_window.py`

- `process_tray_actions` obtains the controller tray-error result, calls `configure_rows`, then evaluates restart scheduling. A rendering failure must still be logged by the existing outer handler; normal success reaches scheduling.
- quota transport error updates `QuotaState`, preserves last-good data when present, calls `render_status`, and continues. It never displays raw exception text.

## RED definitions

1. A mapped production `Pet` at 80% with approved rows fails because root requested height is 111 while actual is 110 and reset row requested height 19 exceeds actual 18.
2. A queued `tray_error` fails because one exception is logged, row values remain unchanged, and restart is not scheduled.
3. A current-generation queued quota transport error fails because one exception is logged and row values remain unchanged even though domain state becomes unavailable.

Each check directly reproduces the observed production path rather than importing only a helper.

## Regression surface

- Pure scale metrics and legacy scale migration.
- Mapped five-row content at 80/100/150/200%.
- Apply/Save/Close/Restore Defaults and config protection.
- Compact/Expand and Hide/Show at current scale.
- Normal, stale, unavailable, tray error/recovery, transport error with/without last-good data.
- Refresh generation/single-flight behavior and shutdown with scheduled callbacks.
- Context menu, settings dialog, tray actions, and package launcher.

## Security, privacy, and resource impact

- No network, provider, token, auth, telemetry, backend, process, worker, polling, dependency, or configuration change.
- Raw transport exception text is no longer rendered, reducing accidental diagnostic-data exposure.
- Two constant-time padding lookups refine existing metric derivation; presentation mapping remains constant-time and main-thread-only.

## Historical Design Verification — invalidated

```text
DESIGN INVALIDATED BY PRODUCTION SCREENSHOT
Problem evidence: PASS
Root-cause hypothesis: PASS
Observable contract: PASS
Failure paths: PASS
Regression surface: PASS
RED definition: PASS
Scope bounded: PASS
Human verification required: NONE
```

The original test and design were insufficient because they instantiated `Pet` without production DPI initialization. The following revision replaces the clipping portion.

## DPI-aware clipping revision

### Contradictory production evidence

Tom's screenshot shows a 264×110 overlay at saved 80% scale with the Reset Credit row visibly cut off. An isolated probe executed the real startup order:

```text
enable_dpi_awareness()
→ create production Pet
→ apply saved 80% scale
→ render screenshot-equivalent five rows
→ map and inspect Tk geometry
```

Observed:

```text
window DPI                 120
window actual/requested    264×110 / 205×129
StatusRows actual/requested 193×96 / 134×115
reset row actual/requested 193×4 / 134×23
```

The prior test process was DPI-unaware and measured 96-DPI 19-pixel rows. Production `run()` calls `enable_dpi_awareness()` before creating Tk; Tk therefore renders the same point fonts at 120 DPI while geometry, padding, gap, and wraplength remain unscaled physical pixels. An all-25-step production-order probe showed clipping at every supported scale.

### Revised root cause

`window_scale_percent` correctly defines logical product scale, but `WindowMetrics` is used simultaneously as logical persisted compatibility data and as physical Tk pixel geometry. Font sizes are points and Tk scales them automatically for DPI; geometry and pixel layout metrics are not automatically scaled. The mixed unit ownership makes the 96-DPI test pass while the 120-DPI product clips.

### Revised approaches

#### A. DPI-aware display metrics with logical persistence — selected

- Extend `derive_window_metrics(value, dpi=96)` so geometry, horizontal/vertical padding, face-text gap, and wraplength scale by `dpi / 96`.
- Keep text/face font sizes as logical point sizes because Tk already applies DPI.
- Keep `derive_window_metrics(value)` as the 96-DPI logical result used for persisted `window_width`, `window_height`, `font_size`, and rollback compatibility.
- In `Pet._sync_compatibility_metrics`, derive logical metrics for settings fields and display metrics from `dpi_for_window(self.winfo_id())` for Tk.
- Use logical content-safe padding `{80: 7, 95: 7, 115: 11}`. At DPI120 it becomes physical `{9, 9, 14}` and all 25 measured steps fit.
- Validate the production startup order in an isolated subprocess so DPI awareness cannot contaminate or be skipped by the main unittest process.

This preserves one canonical user scale, fixed aspect ratio, schema-1 compatibility, automatic font DPI behavior, and existing 96-DPI results.

#### B. Force Tk to 96-DPI font scaling — rejected

Overriding global `tk scaling` would make the old geometry fit but would shrink every Tk point font and affect settings/menu/dialog typography. It fights Windows accessibility scaling rather than making the pixel layout coherent.

#### C. Add arbitrary physical height — rejected

Increasing only height would distort aspect ratio, fail at other scales/DPI values, and retain mixed logical/physical units. The all-step evidence disproves a one-off height fix.

### Revised interfaces

```python
derive_window_metrics(value, dpi=96) -> WindowMetrics
```

- `dpi` accepts a finite positive numeric value; malformed/nonpositive values use 96.
- At 96 DPI, all existing geometry and compatibility results remain unchanged.
- At DPI `D`, physical pixel fields are rounded from the logical result times `D / 96`.
- Point-font fields and `scale_percent` remain unchanged.

`Pet._sync_compatibility_metrics(settings)` performs:

```text
logical = derive_window_metrics(window_scale_percent)
display = derive_window_metrics(window_scale_percent, dpi_for_window(winfo_id))
persist compatibility fields from logical
self.window_metrics = display
```

No second persisted scale or configuration field is introduced.

### Revised observable contract

- A fresh subprocess calls `enable_dpi_awareness()` before any Tk root.
- The subprocess confirms effective window DPI is 120 on the current supported host.
- At every 5% step from 80 through 200, actual geometry equals DPI-scaled logical geometry.
- Root, `StatusRows`, and every label requested height fit allocated height.
- The complete Reset Credit row is visible and single-line.
- 96-DPI pure/UI behavior and persisted compatibility fields remain unchanged.
- Apply/Save/Close/Defaults, Hide/Show, Compact/Expand, position recovery, menu, and restart persistence continue to consume display metrics without changing logical config values.
- Error-presentation GREEN evidence remains valid and independent.

### Revised RED definition

An isolated production-order DPI probe at saved 80% must fail before the fix with:

```text
dpi = 120
window actual height = 110
window requested height = 129
reset row actual/requested height = 4/23
```

The regression must fail because content does not fit, not because DPI awareness or Tk is unavailable. A pure API test must also fail because `derive_window_metrics(80, dpi=120)` is not yet supported.

### Regression surface

- All 25 scales at effective host DPI120 in an isolated production-order process.
- Pure display metrics at 96/120/144/192 DPI.
- Logical schema-1 persistence and v0.3.2 rollback fields.
- Settings transaction semantics and current scale slider.
- Compact/Expand, Hide/Show, position recovery, and menu placement.
- Existing tray/quota error integration and raw-error privacy contract.

### Revised Design Verification

```text
DESIGN VERIFIED
Problem evidence: PASS — real screenshot and production-order DPI120 geometry reproduce clipping
Root-cause hypothesis: PASS — Tk point fonts scale; physical pixel metrics do not
Observable contract: PASS — isolated production-order all-step content fit
Failure paths: PASS — saved logical scale → DPI display metrics → Tk allocation → five rows
Regression surface: PASS
RED definition: PASS — current 80% reset row is 4/23 px
Scope bounded: PASS — existing metrics/API/UI owners only; no schema/dependency/feature
Human verification required: NONE
```
