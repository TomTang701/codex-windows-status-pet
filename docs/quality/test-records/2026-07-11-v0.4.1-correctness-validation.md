# v0.4.1 Correctness Validation — 2026-07-11 (Invalidated)

> **Status:** Invalidated by Tom's real production screenshot on 2026-07-11. The screenshot shows the Reset Credit row still clipped. The mapped-Tk content-fit result below is retained as false-positive evidence and must not be used for release approval. Phase 1 is reopened.

## Candidate

- Branch: `fix/v0.4.1-correctness`
- Runtime-validated commit: `21774c348400d8cc670194571837435d541c2909`
- Application version: `0.4.1`
- Host: Windows 11 x64, two 96-DPI displays
- Scope: status-row content fit and tray/quota error presentation only

## Reproduced RED evidence

### Content clipping

The mapped production `Pet` at 80% allocated a 264×110 window while Tk requested 111 px. `reset_credit` received 18 px against a 19 px request. An expanded all-step probe also found clipping at 95%: 131 px allocated against 135 px requested because text font and vertical padding independently rounded up.

### Tray error

A production `tray_error` queue action logged `tray action failed`, left all five rows unchanged, and did not schedule recovery because `StatusRows.config(text=..., fg=...)` raised before scheduling.

### Quota transport error

A current-generation quota error payload logged `UI refresh payload failed` and left visible rows unchanged after domain state became `unavailable`, because the same Label-only options were sent to the `StatusRows` frame.

## GREEN evidence

### All supported scale steps

`tests.test_ui_content_fit.ContentFitTests.test_supported_scales_allocate_every_requested_row` maps the production window at every 5% step from 80% through 200% with:

```text
Codex 输出中
活动对话 1 个
5h 10% / 17:23
周 86% / 12:23 7/17
重置 5 次 / 18:40 7/12
```

At every step:

- actual geometry equals the existing canonical rounded geometry;
- root and `StatusRows` requested heights fit allocated heights;
- all five labels have positive height and requested height no greater than actual height;
- every row bottom remains inside the status container;
- the complete reset-credit row fits horizontally and vertically.

Only measured vertical padding changes: 80% uses 7 px and 95% uses 8 px. Geometry remains 264×110 through 660×276. Legacy geometry migration and derived compatibility fields retain their prior values.

### Error presentation

- Tray error now produces activity `Codex`, progress `托盘图标异常`, color `#fca5a5`, no logged Tcl error, and one scheduled restart.
- Quota transport error without last-good data produces progress `额度暂不可用`, color `#fca5a5`, `QuotaState.state == "unavailable"`, no logged Tcl error, and no raw exception text.
- Existing recent-last-good and stale rules remain covered by `tests.test_quota_state`.
- Source search finds no remaining `self.text.config(text=...)` call.

## Automated gates

- Version-source consistency: passed at `0.4.1`.
- Routine Quality: approved.
- Core tests: 128 passed.
- Tk UI tests: 20 passed.
- Document manifest/governance/links/parity: passed for 17 registered bilingual pairs.
- Sensitive-file and dependency checks: passed.
- Package smoke: passed.
- Strict compatibility readiness: passed with existing declared physical limitations.
- Strict Release Candidate: approved.

## Windows host runtime

Root launcher observation for exact commit `21774c348400d8cc670194571837435d541c2909`:

- one project `pythonw.exe` process;
- command line targeted this repository's `scripts/codex_status_pet.py`;
- no persistent launcher CMD;
- main title `Codex Windows Status Pet`;
- nonzero HWND `0x2B40ABC`;
- display probe resolved the same HWND at 96 DPI;
- virtual desktop `(0, 0, 4480, 1434)` with two monitors;
- bottom taskbar detected.

The execution harness may clean descendant processes after a terminal command ends; validation therefore collected process, window, and display evidence inside one command lifetime. No application crash was recorded.

## Limitations

- Mixed-DPI physical hardware remains unavailable; simulated DPI tests are not represented as physical evidence.
- Top/left/right physical taskbar edges and a separate clean Windows machine remain unavailable and are not claimed.
- No human visual confirmation was required; every v0.4.1 fact above was obtained through source, tests, Tk, process, Win32, filesystem, or repository inspection.
