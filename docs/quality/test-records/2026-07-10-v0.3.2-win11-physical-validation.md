# v0.3.2 Windows 11 Physical Validation Record

- Date/time: 2026-07-10, America/Los_Angeles
- Tester: maintainer with Codex-assisted observation
- Version target: `0.3.2`
- Branch: `release/v0.3.2-windows11-stabilization`
- Base commit: `d4a69e9ce4a6adc7d519ff1a37b00617d548e8dd`
- Checkpoint commit: `2d493ed2c2b6094ac555913db4cf2c04f9388e0f`

## Confirmed physical environment

- OS: Microsoft Windows 11 Home, x64, version `10.0.26200`, build `26200`.
- Displays: two connected monitors.
- DPI: both monitor probes reported 96 x 96 DPI.
- Virtual desktop: `(0,0)` through `(4480,1434)`.
- Work areas: `DISPLAY1 (0,0)-(2048,1104)` and `DISPLAY2 (2560,354)-(4480,1386)`.
- Taskbar: physically present at the bottom; probe rectangle `(0,1380)-(2560,1440)`.
- Overlay: `FindWindowW` returned a nonzero HWND; `probe_display.py` reported overlay DPI 96.

## Confirmed runtime and visual observations

- The root CMD launcher started the application without leaving a persistent CMD window.
- Two immediate repeated launches briefly created startup processes; after the startup/mutex settling interval, exactly one actual project `pythonw.exe` process remained.
- The surviving process command line pointed to this repository's `scripts/codex_status_pet.py`.
- The application window was visible.
- Five independent status rows were visibly present.
- A Reset Credit line with time and date was visibly present in the supplied physical checkpoint.

## Not yet claimed by this checkpoint

- Genuine production-idle Compact shrink and pointer-hover expansion.
- Full context-menu interaction sequence.
- Single-monitor physical topology.
- Mixed-DPI physical certification.
- Separate clean-machine startup.

These unconfirmed items are not labeled as physical passes. Their final resolution or approved limitation is recorded in the compatibility matrix before release.

## Strict blocker inventory before closure

| Matrix row | Current status | Why blocked | Required evidence | Planned resolution |
|---|---|---|---|---|
| Displays / Single monitor | Automated + partial physical | No one-display physical run is recorded. | Maintainer-approved temporary single-display run, or honest unresolved limitation. | Await explicit approval; prefer physical pass. |
| Popup / Four corners and taskbar work area | Automated + partial physical | Bottom taskbar is physical, but the row also requests unsupported alternate edges. | Reconfirm current five-item menu on bottom taskbar; separate alternate edges as not claimed/non-blocking. | Manual current-build check plus truthful row split. |
| DPI / 100/125/150/200% | Automated partial | Both connected displays are 96 DPI. | Mixed-DPI hardware or an explicit non-blocking limitation. | Retain simulations and record mixed-DPI physical certification as not claimed/non-blocking. |
| Compact mode / Idle shrink and hover expand | Automated partial | The active Codex session prevents genuine production idle. | Genuine idle shrink, hover expansion, second shrink, and five-row return. | Await a genuine idle period; do not add a debug path or consume quota. |
| Dependencies / Bundled runtime and fallback | Automated partial | No separate clean Windows machine exists. | Fresh environment install, Quality, package smoke, launcher smoke; separate-machine limitation remains explicit. | Use isolated environment and classify separate clean machine as an approved non-blocking distribution limitation. |

## Fresh temporary environment evidence

- Created a new isolated Python 3.12 virtual environment under ignored build output.
- Installed only `requirements.txt`: Pillow 12.3.0, pystray 0.19.5, and transitive six 1.17.0.
- Routine Quality passed: 115 core tests and 12 Tk integration tests.
- Dependency, compile, manifest, governance, link, bilingual parity, version, sensitive-file, and startup-audit checks passed.
- Package smoke produced a nonempty ZIP successfully.
- This is isolated-environment evidence, not a claim that a separate clean Windows machine was physically tested.

## Reproduction commands

```powershell
python scripts/probe_display.py
cmd /d /c start_codex_status_pet.cmd
```

Process inspection used the exact project script name and `pythonw.exe`; it excluded the inspecting shell process. No tokens, prompts, responses, session content, browser history, or unrelated desktop content are recorded.
