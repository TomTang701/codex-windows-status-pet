# v0.5.4 Position Persistence Investigation

## Closure status

**CLOSED INVESTIGATION / SYMPTOM NO LONGER REPRODUCIBLE / NO PROVEN PRODUCTION DEFECT / NO PRODUCT RELEASE.** Design Verification remains failed correctly: no production implementation plan or production behavior change is permitted because the required current-source `drag -> normal tray Exit -> restart` RED was not established. Tom confirmed that restarting the software no longer loses the saved window position.

## Provenance

The inspected running application was one bundled `pythonw.exe` process started by the repository launcher:

```text
codex-windows-status-pet/scripts/codex_status_pet.py
```

Its root window was visible at `(4183, 1262)-(4480, 1386)`, with size `297x124`, on the secondary display work area `(2560, 354)-(4480, 1386)`. The persisted settings file contained the same `(4183, 1262)` coordinates at 90% scale.

## Exact A-path trace

The production handlers were exercised with two `Pet` lifetimes sharing one temporary settings file:

```text
normal unlock
-> production start_drag()/drag()
-> scheduled callback settlement beyond the two-second recovery interval
-> finish_drag()
-> JSON
-> tray action "exit"
-> process_tray_actions()
-> close()
-> JSON
-> Pet B load_settings()
-> safe_position()
-> final root
```

The trace used the current 90% dimensions and a legal secondary-display target. The stable post-drag root was `(4143, 1182)` and every required boundary preserved it:

| Boundary | Coordinates |
| --- | --- |
| Pet A stable root and runtime settings | `(4143, 1182)` |
| JSON after `finish_drag` | `(4143, 1182)` |
| JSON after normal tray Exit / `close` | `(4143, 1182)` |
| Pet B raw loaded settings | `(4143, 1182)` |
| Pet B `safe_position` input/output | `(4143, 1182)` / `(4143, 1182)` |
| Pet B final stable root | `(4143, 1182)` |

The machine-readable trace is generated at `.build/v054-position-persistence-trace.json` by `tests/test_ui_position_persistence.py`.

## Current conclusion

There is no measured first coordinate divergence in the current source path. The direct Windows automation window inventory also does not enumerate the overlay because the protected v0.5.3 tool-window Shell identity keeps it outside ordinary application-window enumeration; it cannot safely drive the real overlay pointer path in this environment.

The exact round-trip test currently passes, so the required answer to the release-blocking question is **No**: this test would not have blocked the current build before the reported symptom. No v0.5.4 production fix, tag, GitHub Release, or product version is justified.
